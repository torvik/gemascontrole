<%# Conditionally set remote: true. Also, passing model name as data attribute %>
<% modal ||= false %>
<% remote = modal ? true : false %>

<%= form_for(@company, remote: remote, html: {role: :form, 'data-model' => 'company'}) do |f| %>
           <% if @company.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>
                <ul>
                <% @company.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
                </ul>
              </div>
            <% end %>


            <div class="form-group" >
              <%= f.label :nome , class: 'control-label'%>
              <%= f.text_field :name , class: "form-control" , placeholder: "Nome da Empresa, exemplo Gemas Control Ltda "%>
              <span class="help-block"></span>
            </div>
            <div class="form-group">
              <%= f.label :email , class: 'control-label'%>
              <%= f.text_field :email, class: "form-control" , placeholder: "Cadastrar como o exemplo gemascontrol@gemascontrol.com.br"   %>
              <span class="help-block"></span>
            </div>
            <div class="form-group">
              <%= f.label :endereço , class: 'control-label'%>
              <%= f.text_field :endereco, class: "form-control"  , placeholder: "Cadastrar como o exemplo Rua Teste 120" %>
              <span class="help-block"></span>
            </div>

             <div class="form-group">
              <%= f.label :cep , class: 'control-label'%>
              <%= f.text_field :cep, class: "form-control"  , placeholder: "Cadastrar como o exemplo 00000000" %>
              <span class="help-block"></span>
            </div>

            <div class="form-group">
              <%= f.label :cidade , class: 'control-label'%>
              <%= f.text_field :cidade, class: "form-control" , placeholder: "Cadastrar como o exemplo Rio de Janeiro" %>
              <span class="help-block"></span>
            </div>
            <div class="form-group">
              <%= f.label :estado , class: 'control-label'%>
              <%= f.text_field :estado, class: "form-control"  , placeholder: "Cadastrar como o exemplo Rio de Janeiro" %>
              <span class="help-block"></span>
            </div>
            <div class="form-group">
              <%= f.label :telefone , class: 'control-label'%>
              <%= f.text_field :telefone_1, class: "form-control" , placeholder: "Cadastrar como o exemplo 2199999999"  %>
              <span class="help-block"></span>
            </div>
            <div class="form-group">
              <%= f.label :celular , class: 'control-label'%>
              <%= f.text_field :telefone_2 , class: "form-control" , placeholder: "Cadastrar como o exemplo 2199999999" %>
              <span class="help-block"></span>
            </div>

            <div class="modal-footer">
              <div class="actions">
              <button type="button" class="btn btn-default" data-dismiss="modal">Fechar</button>
                <%= f.submit "Salvar",  class: "btn btn-primary" %>
              </div>
            </div>

          <% end %>







